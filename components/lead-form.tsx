"use client";
import { FormEvent,useState } from "react";
import { useRouter } from "next/navigation";
export function LeadForm({auditId,websiteUrl}:{auditId:string;websiteUrl:string}){const router=useRouter();const [pending,setPending]=useState(false);const [error,setError]=useState("");async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();setPending(true);const data=new FormData(event.currentTarget);const payload={auditId,websiteUrl,name:data.get('name'),phone:data.get('phone'),email:data.get('email'),consent:data.get('consent')==='on',company:data.get('company')};const response=await fetch('/api/leads',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(payload)});if(response.ok)router.push('/thank-you');else{setError('לא הצלחנו לשמור את הפרטים. בדקו את השדות ונסו שוב.');setPending(false)}}return <form className="lead-form" onSubmit={submit}><div className="field"><label htmlFor="lead-name">שם מלא</label><input id="lead-name" name="name" required minLength={2}/></div><div className="field"><label htmlFor="lead-phone">טלפון</label><input id="lead-phone" name="phone" required type="tel" inputMode="tel"/></div><div className="field full"><label htmlFor="lead-email">אימייל</label><input id="lead-email" name="email" required type="email"/></div><div className="honeypot" aria-hidden="true"><label>Company<input name="company" tabIndex={-1} autoComplete="off"/></label></div><label className="checkbox full"><input type="checkbox" name="consent" required/><span>אני מסכים/ה שיחזרו אליי בנוגע לבדיקה ולשירות.</span></label>{error&&<p className="form-error full" role="alert">{error}</p>}<button className="button full" disabled={pending}>{pending?'שולחים…':'אשמח שיחזרו אליי'}</button></form>}


"use client";
import { useEffect,useState } from "react";
import Link from "next/link";
type ReportData={report:{executiveSummary:string;findings:Array<{id:string;category:string;title:string;description:string;evidence:string;affectedUrl:string;severity:string;confidence:string;impactType:string;effort:string;recommendation:string;source:string}>;quickWins:string[];thirtyDayPlan:Array<{week:number;actions:string[]}>};estimatedValue:{disclaimer:string}};
export function FullReport({id}:{id:string}){const [data,setData]=useState<ReportData|null>(null);const [locked,setLocked]=useState(false);useEffect(()=>{fetch(`/api/audits/${id}/full`,{cache:'no-store'}).then(async response=>{if(response.status===402){setLocked(true);return null}if(!response.ok)throw new Error();return response.json()}).then(value=>value&&setData(value)).catch(()=>setLocked(true))},[id]);if(locked)return <div className="state-page"><span className="eyebrow">הדוח נעול</span><h1>נדרש תשלום מאומת כדי לצפות בדוח המלא</h1><p>הדוח המלא אינו נשלח לדפדפן לפני אישור התשלום בצד השרת.</p><Link className="button" href={`/audit/${id}/results`}>חזרה לתוצאות</Link></div>;if(!data)return <div className="state-page"><div className="loader"/><p>טוענים את הדוח המאובטח…</p></div>;return <section className="report-page"><div className="container"><div className="report-hero"><span className="eyebrow">דוח מלא</span><h1>תוכנית העבודה לאתר</h1><p>{data.report.executiveSummary}</p></div><div className="report-columns"><div><section className="report-section"><h2>Quick wins</h2><ol className="quick-list">{data.report.quickWins.map(item=><li key={item}>{item}</li>)}</ol></section><section className="report-section"><h2>כל הממצאים</h2><div className="full-findings">{data.report.findings.map(finding=><article key={finding.id}><div className="finding-meta"><span>{finding.category}</span><span className={`pill ${finding.severity}`}>{finding.severity}</span><span className="pill">ביטחון {finding.confidence}</span><span className="pill">מאמץ {finding.effort}</span></div><h3>{finding.title}</h3><p>{finding.description}</p><dl><div><dt>ראיה</dt><dd>{finding.evidence}</dd></div><div><dt>איך מתקנים</dt><dd>{finding.recommendation}</dd></div><div><dt>עמוד</dt><dd>{finding.affectedUrl}</dd></div></dl></article>)}</div></section></div><aside className="plan-card"><h2>תוכנית ל־30 יום</h2>{data.report.thirtyDayPlan.map(week=><div key={week.week}><span>שבוע {week.week}</span><ul>{week.actions.map(action=><li key={action}>{action}</li>)}</ul></div>)}<small>{data.estimatedValue.disclaimer}</small></aside></div></div></section>}

